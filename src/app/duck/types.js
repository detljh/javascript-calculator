const OPERATOR = 'OPERATOR';
const OPERAND = 'OPERAND';
const ZERO = 'ZERO';
const DECIMAL = 'DECIMAL';
const EVALUATE = 'EVALUATE';
const CLEAR = 'CLEAR';

export default {
    OPERATOR,
    OPERAND,
    ZERO,
    DECIMAL,
    EVALUATE,
    CLEAR
};